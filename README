本次自己实现单向链表,总结了注意问题：
1.没有将测试代码与类的实现代码分离，开始的时候是分成了两个文件，编译没问题，但是链接时出了问题
    因为CMake语法还不会用，因此暂时搁置。事实上，在新建文件的时候根据是测试、class file、还是head file选择好之后
    自动生成的CmakeList.txt是不需要修改即可使用的
2.类外定义函数时应该加上类模板名，即如下形式
    template <typename T>
    list_node<T> * list<T>::insert(list_node<T> *pos, T t)
    list<T>中的<T>也要写上
3.因为要考虑到链表为空时begin与end返回的都是尾后迭代器，因此初始时head与tail都指向了一个空节点作为尾后节点
    在向空链表插入第一个新节点时要考虑将head指针更新
4.如果链表的空白节点不是动态分配的，而是作为一个成员，则析构函数的最后一句delete tail;不能要，
    否则会将blank这个对象释放两次
4.后续有时间会继续完善其余成员函数
